# curly-rotary-phone-chainlink

Build a fully working Chainlink Functions integration that:
- Executes off-chain data securely
- Fetches and/or computes external data deterministically
- Returns encoded results to a smart contract
- Stores and exposes the result on-chain

our test net- sepolia and our wallet is metamask.
write a simple smart contract mint the token once it has data.